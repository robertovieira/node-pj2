{{ >partials/header }}
    
<h2>Qual seu nome?</h2>

<form method="POST" action="/nome_res">
    <input type="text" name="name" placeholder="Digite seu nome"><br>
    <input type="submit" value="Enviar">
</form>

<h2>Lista de Produtos</h2>
<ul>
{{ #products }}
    <li>{{ title }} - R$ {{ price }}</li>
{{ /products }}
</ul>

<hr>

<h2>Lista de Produtos a partir de R$ 12,00</h2>
<ul>
    {{ #expensives }}
        <li>{{ title }} - R$ {{ price }}</li>
    {{ /expensives }}
    </ul>

{{ >partials/footer }}